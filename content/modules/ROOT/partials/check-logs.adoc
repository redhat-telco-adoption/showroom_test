Check container logs:

[.console-input]
[source,bash,subs="attributes+"]
----
oc logs -l app=<app-name> -n {user}-lab
----

For crashed containers, check previous logs:

[.console-input]
[source,bash,subs="attributes+"]
----
oc logs -l app=<app-name> -n {user}-lab --previous
----
